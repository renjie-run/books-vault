待整理
![](assets/temp/file-20260618152442173.png)
请参考“temp/目录下的原生 HTML文件，将其中的设计系统与页面样式完整还原到当前 Next.js 项目中，要求 尽可能做到视觉、布局、间距、颜色、字体、交互状态一致。

具体要求：

1、先分析 temp/中的 HTML/CSS 结构，提取可复用的设计系统，包括：
- 颜色、字体、间距、圆角、阴影等设计变量
- Button、Card、Input、Navigation、Layout 等通用组件
- 页面级布局与响应式规则

2、在项目中建立清晰的前端架构：
- 尽可能抽象可复用组件
- 避免在页面中重复堆叠样式
- 页面应基于已有组件和设计规范开发

3、编写‘AGENTS.md’文件，用于说明：
- 项目整体介绍
- 前端目录结构
- 设计系统架构
- 组件复用规范
- 后续开发注意事项

4、在‘AGENTS.md’中明确开发规范：
- 开发页面时，必须优先复用已有组件
- 如果已有组件可以通过 props、variant、className 等方式扩展，应优先扩展，而不是重新创建相似组件
- 只有在现有组件无法满足需求时，才新增组件

5、新增一个"设计系统预览"页面：
- 仅用于开发环境查看
- 采用纯前端渲染
- 展示颜色、字体、按钮、卡片、表单、布局等核心组件
- 用于验证组件复用效果和样式还原质量

最终目标：
1. 将'temp/中的前端完整在当前Next.js中还原
2. 将 temp/中的前端迁移为 Next.js 项目中的可维护、可复用、可扩展的前端组件体系
